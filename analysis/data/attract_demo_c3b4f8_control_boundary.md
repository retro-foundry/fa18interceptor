# Demonstration-flight `$C3B4F8` control boundary

Classification: **event-faithful bounded negative control-transition evidence**.

The sealed Demonstration Flight replay reaches `$C1CC70` at host frame 139
with descriptor field `$C3B4F8`.  A 12,000-instruction trace begins at that
exact store after the menu-selection input has completed.  It contains no
later `$C1EF10` cursor publication, `$C1F6F8` walker entry, `$C1F70E`
control-stream load, `$C1F4B0` immutable-transform-source read, `$C212B0`
line submission, or `$C2FF48` polygon submission.

This excludes using that particular bounded execution as a direct ownership
path from `$C3B4F8` to the nearby demo `$C3924C` or `$C34A5E` streams.  It
does not show that `$C3B4F8` can never select a renderer path in another
frame or mode, and it assigns no mountain, building, road, or landmark name.

Authority: `build/attract_demo_c3b4f8_selected_trace/trace.jsonl` and
`trace_summary.json`, generated by `scripts/trace_selected_descriptor_control_target.py`
from sealed `captures/attract_run001` with target `$C3B4F8`.
