# Demonstration-flight `$C44500` to `$C392A8` control path

Classification: **event-faithful bounded descriptor-control-to-primitive path**.

From the sealed Demonstration Flight frame-500 checkpoint, the trace reaches
`$C1CC70` at relative frame 16 reading descriptor field `$C44500`.  In the
following 16,000 instructions, `$C1EF10` publishes `$C3924C` (trace index
5,073), and `$C1F70E` later loads that exact stream into `A5` after the
`$C1F6F8` walker entry.  The same bounded interval reaches `$C2FF48` three
times and `$C212B0` once with `A5=$C392A8`.

```text
$C44500 descriptor field
  -> $C1EF10 cursor $C3924C
  -> $C1F6F8 walker / $C1F70E A5=$C3924C
  -> $C392A8: three polygon submissions and one line submission
```

No `$C1F4B0` immutable-transform-source read occurs in this interval.  Thus
this is direct control-to-primitive evidence for the `$C3924C/$C392A8`
family, not a source-mesh export, placement record, global location, or
building/landmark identification.

Authority: `build/attract_demo_c44500_from500_selected_trace/trace.jsonl`
and `trace_summary.json`, generated by
`scripts/trace_selected_descriptor_control_target.py` from the sealed
frame-500 demo checkpoint.
