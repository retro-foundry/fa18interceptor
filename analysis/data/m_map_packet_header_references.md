# Stable references to M-map packet-header candidates

Classification: **static longword-reference scan**.

A match proves only a stable longword equal to a candidate header. It does not prove the referencer executes, is a map selector, or that an unreferenced candidate is invalid; relative-offset tables and computed pointers are outside this scan.

The scan checks 71 candidate headers in two snapshots. It finds stable external longword references for 0 candidates.

| Header | Live direct entry | Live selector sample | Stable external references |
| --- | --- | --- | --- |
| none | n/a | n/a | none |
