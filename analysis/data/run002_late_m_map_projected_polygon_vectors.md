# Later run002 M-map projected polygon vectors

[Open the source-vector SVG](../visuals/run002_late_m_map_projected_polygon_vectors.svg)
or its [inspection PNG](../visuals/run002_late_m_map_projected_polygon_vectors.png).

From the sealed run002 frame-5171 later Base-4 free-flight checkpoint, one `M`
key press produces a 43-submission map pass. The direct `$C4B390` polygon
export matches its M screen at **112,001 / 113,036 = 99.0844%** for land/sea
pixels.

The blue-water coastline joins run035 as `run035(x-120,y+61)=run002_late(x,y)`
at 98.8856% agreement over 60,840 pixels, placing this pass at `(10,122)` in
the normalized mosaic. It overlaps the earlier Base-4 pass but directly
extends its upper/left edge; it does not establish an absolute position or
landmark identity.

Authority: sealed run002 checkpoint, with ignored reproduction captures in
`build/run002_late_m_map_projected_polygons/` and `build/run002_late_m_map_oracle/`.
