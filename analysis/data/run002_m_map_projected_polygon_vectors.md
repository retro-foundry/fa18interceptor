# Run002 M-map projected polygon vectors

[Open the source-vector SVG](../visuals/run002_m_map_projected_polygon_vectors.svg)
or its [inspection PNG](../visuals/run002_m_map_projected_polygon_vectors.png).

From the sealed run002 frame-4302 Base-4 free-flight checkpoint, one `M` key
press produces a 43-submission map pass. The direct `$C4B390` polygon export
matches its M screen at **112,073 / 113,102 = 99.0902%** for land/sea pixels.

The blue-water coastline joins run035 as `run035(x-114,y+70)=run002(x,y)` at
97.9792% agreement over 56,808 pixels, placing this pass at `(16,131)` in the
normalized mosaic. It contributes new observed coastline coverage down and
left of the earlier joined views; it does not establish absolute map
coordinates or a named landmark.

Authority: sealed run002 checkpoint, with ignored reproduction captures in
`build/run002_m_map_projected_polygons/` and `build/run002_m_map_oracle/`.
