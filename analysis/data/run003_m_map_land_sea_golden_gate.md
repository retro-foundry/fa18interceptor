# Run003 M-map land/sea review

[Open the coloured review SVG](../visuals/run003_m_map_land_sea_golden_gate.svg).
It shows the requested readable green land and blue sea presentation, using the
exact run003 M-screen oracle.  The **Golden Gate** callout is overlaid at its
separately traced `$C3559A/$C355D2` renderer-vector anchor.

The fills and landmark have intentionally different provenance: land/sea are
the pixel-perfect visual target; Golden Gate is source/control evidence.  This
does not claim that the filled shoreline has already been recovered as original
static polygon data.  That extraction remains the active renderer work.
