# Run004 M-map projected polygon vectors

[Open the source-vector SVG](../visuals/run004_m_map_projected_polygon_vectors.svg)
or its [inspection PNG](../visuals/run004_m_map_projected_polygon_vectors.png).

From the sealed run004 frame-2570 Base-3 free-flight checkpoint, one `M` key
press enters a pass with a two-submission non-map setup prefix followed by 22
observed map polygons. The direct `$C4B390` export matches its M screen at
**112,160 / 112,700 = 99.5209%** for land/sea pixels.

The blue-water coastline joins run035 as `run035(x+226,y+109)=run004(x,y)` at
99.3979% agreement over 28,566 pixels, placing the bounded pass at `(356,170)`
in the normalized mosaic. It contributes new lower-right coverage; it does not
establish absolute coordinates or a named landmark.

Authority: sealed run004 checkpoint, with ignored reproduction captures in
`build/run004_m_map_projected_polygons/` and `build/run004_m_map_oracle/`.
