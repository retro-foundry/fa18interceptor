# Run024 M-map projected polygon vectors

[Open the source-vector SVG](../visuals/run024_m_map_projected_polygon_vectors.svg)
or its [inspection PNG](../visuals/run024_m_map_projected_polygon_vectors.png).

From the sealed run024 frame-23000 San Francisco cockpit checkpoint, one `M`
key press produces a 42-submission map pass. The `$C4B390` polygons captured
immediately before `$C2FF48` agree with the matching M screen at **110,807 /
112,322 = 98.6512%** for land/sea pixels.

The blue-water coastline joins run035 as `run035(x+140,y+35)=run024(x,y)` at
98.1594% agreement over 71,500 pixels, placing this capture at `(270,96)` in
the normalized mosaic. This makes its relationship to the user-verified San
Francisco flight state evidence for map-view continuity, not a proof of a city
landmark coordinate or of marker ownership.

Authority: sealed run024 checkpoint, with ignored reproduction captures in
`build/run024_m_map_projected_polygons/` and `build/run024_m_map_oracle/`.
