# run035 panned-map control probe

Classification: **scenario-backed map display control activity; not terrain or
LOD evidence**.

The sealed run035 map view is the second coastline position used by the
translation comparison. A no-input 13-chipset-frame trace immediately after
its recorded map command contains 134,026 instructions and reaches
`$C1F6F8` three times, at trace frames 8,857, 8,862, and 8,866. Each entry has
`A1=$C3515A`.

Each bounded walker interval emits the same eight `$C2FA7E` line inputs:

```text
(157,72)->(161,72)  (141,90)->(145,90)  (173,90)->(177,90)
(85,144)->(85,45)   (85,45)->(93,36)    (93,36)->(225,36)
(225,36)->(233,45)  (233,45)->(233,144)
```

The long vertical and horizontal bounds make this a map-display/grid/frame
candidate, not a located terrain component. Crucially, the entire 13-frame
trace has **zero** `$C1F4AC` transform entries. No immutable vertex source can
therefore be joined to `$C3515A` in this probe.

Compared with run003's first map-preparation trace, this is a different
control workload at a different panned view, but it is not a LOD result:
world position, map transition timing, and display/control state all differ.
It neither proves a mesh replacement nor links the panned coastline pixels to
the static terrain template lattice.

Authority: sealed `captures/run035`; ignored
`build/run035_m_map_appearance_13f_trace/trace.jsonl`, generated by:

```text
python scripts/engine9000_bridge.py \
  --restore captures/run035/initial_state.bin \
  --config captures/run035/config.uae \
  --playback captures/run035/playback.e9k \
  --frames 8855 --trace-frames 13 \
  --output build/run035_m_map_appearance_13f_trace
```
