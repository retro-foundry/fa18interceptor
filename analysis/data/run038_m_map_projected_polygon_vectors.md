# Run038 M-map projected polygon vectors

[Open the source-vector SVG](../visuals/run038_m_map_projected_polygon_vectors.svg)
or its [inspection PNG](../visuals/run038_m_map_projected_polygon_vectors.png).

From the sealed run038 frame-6750 flight checkpoint, one `M` key press enters
a stable 58-submission map pass after an eight-submission setup prefix. Those
direct `$C4B390` polygons, captured immediately before `$C2FF48`, agree with
the matching M screen at **109,380 / 111,940 = 97.7131%** for land/sea pixels.

The blue-water coastline joins run035 as `run035(x+24,y+10)=run038(x,y)` at
98.2684% agreement over 103,488 pixels. This adds observed source-vector
coverage to the mosaic; it does not identify a terrain source or landmark.

Authority: sealed run038 checkpoint, with ignored reproduction captures in
`build/run038_m_map_projected_polygons/` and `build/run038_m_map_oracle/`.
