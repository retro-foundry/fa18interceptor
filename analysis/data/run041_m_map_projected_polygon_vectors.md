# Run041 M-map projected polygon vectors

[Open the source-vector SVG](../visuals/run041_m_map_projected_polygon_vectors.svg)
or its [inspection PNG](../visuals/run041_m_map_projected_polygon_vectors.png).

From the sealed run041 frame-6000 Golden Gate-approach checkpoint, one M-key
press reaches a moving map renderer schedule after a ten-submission setup
prefix. Submissions 10--59 form the explicitly bounded 50-polygon pass used
here; it agrees with the matching no-input map screen at **109,487 / 111,970
= 97.7824%** for land/sea pixels.

It joins run035 as `run035(x+42,y+11)=run041(x,y)` with 98.2697% blue-water
agreement over 99,866 pixels. Composed with the established run035/run042
join, this places the run041 pass at `(172,72)` in the shared mosaic frame.
This establishes rendered map-view panning, not global coordinates or an
absolute terrain map.
