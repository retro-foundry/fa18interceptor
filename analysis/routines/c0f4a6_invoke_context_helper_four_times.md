# `$C0F4A6` four-call context helper

Classification: **structural with byte-exact source**. The routine calls
`$C17B08` four times with longword stack arguments 0, 1, 2, and 3, cleaning
four bytes after each call. Its caller and helper semantics remain unassigned.
