# `$C1BAD4` context output lookup table

Classification: **byte-exact data**. This 14-byte table is indexed by `$C1BA64` using the signed extension of `$C457A7`.

The publisher stores the selected byte as a word and also shifts it left four bits for a second output field. Its UI meaning is unassigned.
