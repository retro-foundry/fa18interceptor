# `$C1BD78` indexed command-mode selector

Classification: **structural with byte-exact source**. In zero context mode, this routine maps `D4` through direct modes, special values, an indexed record byte, and an offset-validation table before storing `$C458A6` and calling `$C3318E`.

It uses `$C45792` as an observed state marker on selected routes. The mode labels describe dispatch mechanics only; player-control meaning requires bounded input traces.
