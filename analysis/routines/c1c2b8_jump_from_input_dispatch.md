# `$C1C2B8` input-dispatch transfer

Classification: **structural with byte-exact source**. This separate entry is
a direct long jump to `$C06BF0`; it does not return through the preceding
queued-input routine. It is the target of several keyboard-dispatch paths.
