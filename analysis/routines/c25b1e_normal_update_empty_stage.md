# Normal-update empty stage at `$C25B1E`

Classification: **behavioural leaf**. In sealed run003 frame 6,000, the
normal parent calls `$C25B1E` from `$C0F010`; it immediately returns to
`$C0F016`. Canonical P-code is `pcode/raw/run003_6000_c25b1e/` (one
instruction, three operations). The two-byte leaf is byte-exact source in
`source_amiga/observed/normal_update_empty_stage.asm`.
