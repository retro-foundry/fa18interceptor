# `$C2DAE0` matrix auxiliary-word copy

Classification: **byte-exact static reconstruction**.  This is the terminal
helper-sized block of the bounded `$C2D9BA` matrix pipeline. It copies three
words from `$C461EA` through `D0-D2` to `$C45A88` and returns.

The data-field roles remain unassigned. The complete `$C2DAE0-$C2DAF1` range
(18 bytes) is `source_amiga/observed/copy_matrix_auxiliary_words.asm`.
