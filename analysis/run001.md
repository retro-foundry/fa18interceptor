# Human coverage run 001

`captures/run001/` is a sealed replay captured from the preserved post-load
menu state. The player selected Base 1 and plane type 1, took off, used
afterburner, performed a barrel roll, and crashed into the sea. Treat that
description as a scenario index; code and screen conclusions require the
recorded input and replay evidence below.

## Authority

- 256 post-setup events; last event frame 4,441.
- `playback.e9k` SHA-256:
  `afa97588763823cf7ec5be3688887710650a0477568152dabc822b65eca1456b`.
- Ordinary full-frame replay through frame 4,441 completed in 10.826 seconds.
- End-frame video SHA-256:
  `4f096e9fff9e96fb08c9ba65bc91cb0a8c340549f6b8578d88f7bb84ec450862`.

## Coarse input coverage

| Frames | Recorded input types | Use |
| --- | --- | --- |
| 50–1,221 | mouse, keyboard, mouse buttons | menu and setup selections |
| 2,507–2,513 | keyboard | pre-flight transition |
| 2,697–4,212 | joystick | takeoff and flight-control coverage |
| 3,196–3,227 | keyboard | keyboard action during the flight interval |
| 4,344–4,358 | mouse | terminal UI interaction before close |

The run also contains a close event at frame 4,441. These ranges are derived
from the sealed event log, not from inferred control meanings.

## Keyframes

`build/run001_keyframes/` was generated in one ordinary replay pass. The
images show a black/menu-transition frame at 500, an external flight view at
1,500, and cockpit/runway views at 2,800, 3,400, and 4,441. They establish
replay-visible states only; they do not by themselves identify the routines
that produced those states.

Use the first joystick transition at frame 2,697 as the next bounded
input-to-frame investigation point. Do not instruction-step across later
recorded input until a save state is captured immediately before that event.

That state is now saved at `build/run001_pre_joystick_2696/`; the single press
has a rebased, one-event recording at `build/run001_joystick_press_only.e9k`.
It produced the complete `$C1718E` `JOY0DAT` callback packet documented in
`analysis/routines/c1718e_joy0dat_callback.md`.
