# Human coverage run031

`captures/run031/` is a sealed replay captured from the preserved menu state.
It is the broadest scene/model-discovery oracle currently available.

## Authority

- 379 post-setup events; last input frame 15,758.
- `playback.e9k` SHA-256:
  `73ad8801b8890806c5b82f38a1c176e5cf304ef4d812c8160057e146e1577c8b`.
- Initial-state SHA-256:
  `2195460cd9034df634b779a52fd53fcbf88100a37953302b2792c8f204c36ef6`.
- Configuration SHA-256:
  `1db7a42a96ee97bd86f5196741511cffc2c209e9b81a2445e20a292040d53afb`.

## User-supplied scenario index

The operator reports this intended sequence:

1. Select menu item `1` to start the demo.
2. Take off and perform a barrel roll.
3. Acquire and shoot down an enemy, then barrel roll again.
4. Fly under the Golden Gate Bridge.
5. See Alcatraz, fly over San Francisco, then fly under a second bridge whose
   name is not yet supplied.
6. Use both internal and external camera views during the flight.

These are scene-identification leads, not model, collision, or code-name
assignments. The known aircraft, enemy, landmark, and view transitions make
this replay suitable for controlled state and renderer comparisons.

## Coarse visual anchors

`build/run031_keyframes/` is one ordinary replay pass at 500-frame intervals.
The following are visible facts from its coarse samples:

| Frame | Visible state | Evidence limit |
| ---: | --- | --- |
| 1,500 | Mission-selection menu | Before the reported demonstration flight. |
| 4,500 | Cockpit view with `ALERT: ENEMY IS VICINITY` | Enemy alert is visible; no target record is assigned. |
| 7,500 | External view of the player aircraft | Aircraft silhouette is visible; its model stream remains unassigned. |
| 11,000 | Cockpit landscape / water view | No landmark identity is inferred from this coarse frame. |
| 14,500 | Cockpit view with a bridge silhouette | Candidate for one of the reported bridge passes; its identity and data path require a narrower labeled window. |

## Next evidence slice

Obtain user labels for the coarse screenshots, then replay narrow no-input
windows around the confirmed aircraft, enemy, Golden Gate, Alcatraz, city, and
second-bridge frames. Compare control-stream and renderer packets only between
matched camera modes, so a camera overlay does not get confused with scene
geometry.
